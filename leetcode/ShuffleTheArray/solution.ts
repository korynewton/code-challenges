function shuffle(nums: number[], n: number): number[] {
    let half: number = nums.length / 2;
    const shuffled: number[] = [];
    let i: number = 0;
    while (half < nums.length) {
        deshuffled.push(nums[i], nums[half])
        i++;
        half++
    }
    
    return shuffled
};
